{% extends 'base.html.twig' %}
{% block body %}
    <main class="container">
        <h3 class="display-5 my-3">Ajouter une peinture</h3>
        <div class="row">
            <div class="col-12 col-md-8 add-form">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-light">Ajouter</button>
                {{ form_end(form) }}
            </div>
        </div>
    </main>
{% endblock %}
